{
  "name": "stepkit",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "examples:in-memory": "pnpm -C examples/in-memory dev",
    "examples:inngest": "pnpm -C examples/inngest dev"  ,
    "precommit": "pnpm type-check && pnpm test",
    "test": "pnpm -r test",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.5"
  }
}
