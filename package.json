{
  "name": "open-workflow",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "examples:in-memory:start": "pnpm -C examples/in-memory start",
    "examples:inngest:start": "pnpm -C examples/inngest start",
    "precommit": "pnpm type-check && pnpm test",
    "test": "pnpm -r test",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.5"
  }
}
